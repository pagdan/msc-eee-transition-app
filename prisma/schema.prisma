generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}


// User model
model User {
  id            String    @id @default(cuid())
  email         String    @unique
  name          String?
  studentId     String?   @unique
  image         String?
  programme     String?   @default("MSc EEE")
  yearOfStudy   Int?      @default(1)
  emailVerified DateTime?
  createdAt     DateTime  @default(now())
  updatedAt     DateTime  @updatedAt

  accounts      Account[]
  sessions      Session[]
  calendarEvents CalendarEvent[]
  forumPosts    ForumPost[]
  forumComments ForumComment[]
  wellbeingLogs WellbeingLog[]

  @@map("users")
}

// NextAuth Account model
model Account {
  id                String  @id @default(cuid())
  userId            String
  type              String
  provider          String
  providerAccountId String
  refresh_token     String? @db.Text
  access_token      String? @db.Text
  expires_at        Int?
  token_type        String?
  scope             String?
  id_token          String? @db.Text
  session_state     String?

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@unique([provider, providerAccountId])
  @@map("accounts")
}

// NextAuth Session model
model Session {
  id           String   @id @default(cuid())
  sessionToken String   @unique
  userId       String
  expires      DateTime
  user         User     @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("sessions")
}

// Calendar Events
model CalendarEvent {
  id          String   @id @default(cuid())
  userId      String
  title       String
  description String?  @db.Text
  location    String?
  startTime   DateTime
  endTime     DateTime
  isAllDay    Boolean  @default(false)
  category    String?  // "academic", "orientation", "social", "deadline"
  outlookId   String?  @unique // Microsoft Outlook event ID
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("calendar_events")
}

// Club
model Club {
  id          String   @id @default(cuid())
  name        String
  slug        String   @unique
  description String   @db.Text
  category    String   // "Academic", "Social", "Technical", "Professional"
  imageUrl    String?
  memberCount Int      @default(0)
  contactEmail String?
  website     String?
  isActive    Boolean  @default(true)
  instagram   String?
  facebook    String?
  linkedin    String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("clubs")
}

// Forum Posts
model ForumPost {
  id        String   @id @default(cuid())
  title     String
  content   String   @db.Text
  category  String   // "general", "academics", "housing", "social"
  authorId  String
  isPinned  Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  author   User           @relation(fields: [authorId], references: [id], onDelete: Cascade)
  comments ForumComment[]

  @@map("forum_posts")
}

// Forum Comments
model ForumComment {
  id        String   @id @default(cuid())
  content   String   @db.Text
  postId    String
  authorId  String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  post   ForumPost @relation(fields: [postId], references: [id], onDelete: Cascade)
  author User      @relation(fields: [authorId], references: [id], onDelete: Cascade)

  @@map("forum_comments")
}

// Dining Locations
model DiningLocation {
  id          String   @id @default(cuid())
  name        String
  description String?  @db.Text
  location    String
  cuisine     String[]
  priceRange  String   // "$", "$$", "$$$"
  openingHours String?
  imageUrl    String?
  rating      Float?   @default(0)
  isOpen      Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("dining_locations")
}

// Study Spots
model StudySpot {
  id          String   @id @default(cuid())
  name        String
  description String?  @db.Text
  location    String
  capacity    String   // "small", "medium", "large"
  amenities   String[] // ["wifi", "power_outlets", "quiet", "group_study"]
  openingHours String?
  imageUrl    String?
  rating      Float?   @default(0)
  isAvailable Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("study_spots")
}

// Bus Routes
model BusRoute {
  id          String   @id @default(cuid())
  routeNumber String
  routeName   String
  description String?  @db.Text
  schedule    Json     // Store schedule as JSON
  stops       String[]
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("bus_routes")
}

// Wellbeing Resources
model WellbeingResource {
  id          String   @id @default(cuid())
  title       String
  description String   @db.Text
  category    String   // "mental_health", "physical_health", "counseling", "workshops"
  resourceType String  // "article", "video", "service", "event"
  link        String?
  contactInfo String?
  imageUrl    String?
  isActive    Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("wellbeing_resources")
}

// Wellbeing Logs (for students to track their wellbeing)
model WellbeingLog {
  id        String   @id @default(cuid())
  userId    String
  moodScore Int      // 1-5 scale
  notes     String?  @db.Text
  date      DateTime @default(now())
  createdAt DateTime @default(now())

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@map("wellbeing_logs")
}

// Events (for dashboard upcoming events section)
model Event {
  id          String   @id @default(cuid())
  title       String
  subtitle    String   @db.Text
  date        DateTime
  imageUrl    String?
  content     String?  @db.Text // Full event description for detail page
  isPublished Boolean  @default(true)
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("events")
}

// Contact form submissions
model Contact {
  id        String   @id @default(cuid())
  name      String
  email     String
  subject   String
  message   String   @db.Text
  status    String   @default("new") // new, read, resolved
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("contacts")
}
